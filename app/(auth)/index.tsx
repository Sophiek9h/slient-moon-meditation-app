import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const index = () => {
  return (
    <SafeAreaView >
      <Text className='text-3xl text-amber-800'>in or out?</Text>
    </SafeAreaView>
  )
}

export default index